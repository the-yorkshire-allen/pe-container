# pe-container Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-18

## Active Technologies
- Bash 5.x lifecycle scripts, Dockerfile syntax 1.x, GNU coreutils + Docker/OCI build tooling, Puppet Enterprise installer tar.gz artifact, `tini` as PID 1, ShellCheck, Bats (001-containerize-pe)
- Operator-managed persistent storage layer for PE configuration, data, and logs; persisted lifecycle and version markers (001-containerize-pe)
- Operator-managed persistence targets for PE configuration/certs, application data, and logs; persisted lifecycle and version markers (001-containerize-pe)

- Bash 5.x for lifecycle scripts, Dockerfile syntax 1.x for image build + Docker/OCI image build tooling, bundled Puppet Enterprise installer payload, Linux coreutils, `tini` as PID 1, ShellCheck and Bats for script validation (001-containerize-pe)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash 5.x for lifecycle scripts, Dockerfile syntax 1.x for image build

## Code Style

Bash 5.x for lifecycle scripts, Dockerfile syntax 1.x for image build: Follow standard conventions

## Recent Changes
- 001-containerize-pe: Added Bash 5.x lifecycle scripts, Dockerfile syntax 1.x, GNU coreutils + Docker/OCI build tooling, Puppet Enterprise installer tar.gz artifact, `tini` as PID 1, ShellCheck, Bats
- 001-containerize-pe: Added Bash 5.x lifecycle scripts, Dockerfile syntax 1.x, GNU coreutils + Docker/OCI build tooling, Puppet Enterprise installer tar.gz artifact, `tini` as PID 1, ShellCheck, Bats
- 001-containerize-pe: Added Bash 5.x lifecycle scripts, Dockerfile syntax 1.x, GNU coreutils + Docker/OCI build tooling, Puppet Enterprise installer tar.gz artifact, `tini` as PID 1, ShellCheck, Bats


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
